---
import BaseCard from './BaseCard.astro';
import ForwardLink from './ForwardLink.astro';

interface Props {
  title: string;
  slug: string;
  summary: string;
}

const { title, slug, summary } = Astro.props;
---

<BaseCard href={`/frameworks/${slug}`} class="framework-card">
  <div slot="badge">
    <span class="framework-badge">Framework</span>
  </div>
  <Fragment slot="title">{title}</Fragment>
  <Fragment slot="description">{summary}</Fragment>
  <ForwardLink text="Framework details" />
</BaseCard>

<style>
  .framework-badge { color: var(--color-accent); font-size: .6875rem; font-weight: 600; letter-spacing: .08em; text-transform: uppercase; }
</style>
